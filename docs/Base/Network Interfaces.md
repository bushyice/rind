[[Rind]] has an internal basic networking engine that allow to be defined as [[Units]]. It triggers [[States]] for the current machine network state.

### Example
```toml
[[network]]
name = "eth0"
method = "dhcp"
```
